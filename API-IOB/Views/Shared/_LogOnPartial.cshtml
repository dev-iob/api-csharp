@if(Request.IsAuthenticated) {
    <text>Bem-vindo <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Fazer Logoff", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Fazer Logon", "LogOn", "Account") ]
}
